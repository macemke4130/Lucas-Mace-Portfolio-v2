import styled from 'styled-components';
import { font, compColor, mainColor } from "./SSOT.Style";

export const Footer = styled.footer`
    /* padding: 1rem 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center; */
    height: 5rem;
    background-image: linear-gradient(${mainColor}, ${compColor});
`;